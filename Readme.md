# –еализаци€ метода √аусса без выбора ведущего элемента

## ѕостановка задачи

–еализовать метод √аусса без выбора ведущего или главного элемента дл€ решени€ системы вида Ax=f и вычислить определитель матрицы.

¬ходной файл:
+ n Ч размерность матрицы A;
+ элементы матрицы A построчно;
+ вектор f.

¬ыходной файл:
+ IER Ц индикатор ошибки:
    + IER=0 Ч нет ошибки;
    + IER=1 Ч возникло деление на 0 и система не может быть решена методом √аусса.
+ найденное решение системы;
+ погрешность (если можно вычислить);
+ вектор нев€зки.

## —труктура программы

+ ‘айл input.txt Ч входной файл, содержащий размерность матрицы, ее коэффициенты и свободные члены.
+ ‘айл output.txt Ч выходной файл с кодом ошибки, решением системы, погрешностью (дл€ одного теста), вектором нев€зки и определителем системы.
+  ласс Input. —читывает значени€ с файла, хранит их дл€ других классов (статический).
+  ласс Output. «аписывает полученные значени€ в файл.
+  ласс GaussMethod. –еализовывает алгоритм метода √аусса, нахождение определител€, вектора нев€зки, погрешности.
+  ласс Program. —оздает экземпл€ры классов и вызывает необходимые дл€ работы методы.
